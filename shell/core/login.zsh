# shell/core/login.zsh
#
# Shared login shell setup — sourced by each machine's zprofile.
# Runs for LOGIN shells only (after zshenv, before zshrc).
# One-time-per-session setup.
#

# Nothing shared yet — machine zprofiles load their own modules
# (e.g., homebrew, toolbox paths).
# This file exists as the hook point for any future shared login config.

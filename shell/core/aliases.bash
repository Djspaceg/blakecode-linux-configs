# shell/core/aliases.bash
#
# Shared aliases for bash machines.
# Identical to aliases.zsh but sourced from bash configs.
# (This file uses only POSIX-compatible syntax.)
#

source "$PROFILECONFIGDIR/shell/core/aliases.zsh"
